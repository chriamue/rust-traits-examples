name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      checks: write

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install cargo-tarpaulin
        run: cargo install cargo-tarpaulin

      - name: Run tests with coverage
        run: |
          cargo tarpaulin --verbose --all-features --workspace --timeout 120 \
            --out Html --out Xml --output-dir coverage/

      - name: Code Coverage Report
        uses: irongut/CodeCoverageSummary@v1.3.0
        with:
          filename: coverage/cobertura.xml
          badge: true
          fail_below_min: false
          format: markdown
          hide_branch_rate: false
          hide_complexity: true
          indicators: true
          output: both
          thresholds: "60 80"

      - name: Add Coverage PR Comment
        uses: marocchino/sticky-pull-request-comment@v2
        if: github.event_name == 'pull_request'
        with:
          recreate: true
          path: code-coverage-results.md

      - name: Parse coverage results
        id: coverage
        run: |
          COVERAGE=$(grep -oP 'line-rate="\K[0-9.]+' coverage/cobertura.xml | head -1 | awk '{printf "%.1f", $1*100}')
          echo "percentage=$COVERAGE" >> $GITHUB_OUTPUT

          LINES_COVERED=$(grep -oP 'lines-covered="\K[0-9]+' coverage/cobertura.xml | head -1)
          LINES_VALID=$(grep -oP 'lines-valid="\K[0-9]+' coverage/cobertura.xml | head -1)
          echo "lines_covered=$LINES_COVERED" >> $GITHUB_OUTPUT
          echo "lines_valid=$LINES_VALID" >> $GITHUB_OUTPUT

          if (( $(echo "$COVERAGE >= 80" | bc -l) )); then
            echo "badge_color=brightgreen" >> $GITHUB_OUTPUT
            echo "badge_message=excellent" >> $GITHUB_OUTPUT
          elif (( $(echo "$COVERAGE >= 60" | bc -l) )); then
            echo "badge_color=yellow" >> $GITHUB_OUTPUT
            echo "badge_message=good" >> $GITHUB_OUTPUT
          else
            echo "badge_color=red" >> $GITHUB_OUTPUT
            echo "badge_message=needs improvement" >> $GITHUB_OUTPUT
          fi

      - name: Generate detailed test summary
        if: always()
        run: |
          echo "# 🧪 Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "## 📊 Code Coverage" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Coverage** | ${{ steps.coverage.outputs.percentage }}% |" >> $GITHUB_STEP_SUMMARY
          echo "| **Lines Covered** | ${{ steps.coverage.outputs.lines_covered }} / ${{ steps.coverage.outputs.lines_valid }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          COVERAGE=${{ steps.coverage.outputs.percentage }}
          if (( $(echo "$COVERAGE >= 80" | bc -l) )); then
            echo "🟢 **Excellent coverage** (≥80%)" >> $GITHUB_STEP_SUMMARY
          elif (( $(echo "$COVERAGE >= 60" | bc -l) )); then
            echo "🟡 **Good coverage** (≥60%)" >> $GITHUB_STEP_SUMMARY
          else
            echo "🔴 **Coverage needs improvement** (<60%)" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📦 Test Modules" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Animals Module**: Dogs, Ducks, Eagles, Penguins, Snakes, Whales" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Behaviors Module**: Walking, Swimming, Flying, Driving, LandMove" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Vehicles Module**: Cars, Motorcycles, Airplanes, Ships, Helicopters" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Core Module**: Energy, Terrain, Weather, Intensity" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Competitions Module**: Triathlon, Relay, Unified Race, Vehicle Race" >> $GITHUB_STEP_SUMMARY

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage/
